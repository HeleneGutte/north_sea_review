# Analysis for North Sea review ----

# 1. packages and data ----
library(tidyverse)
answers_meta_final <- read_csv("Data/answers_meta_final.csv")

# 2. plotting settings ----
my_colours <- c("invasive species" = "turquoise", "climate change" = "orange", 
                "direct exploitation" = "blue", "sea use change" = "gold", 
                "pollution" = "purple")

# 3. prepare data table ----



